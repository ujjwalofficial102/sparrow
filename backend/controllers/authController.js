export const signup = (req, res) => {
  res.json({
    data: "you hit the signup endpoint from authController",
  });
};
export const login = (req, res) => {
  res.json({
    data: "you hit the login endpoint from authController",
  });
};
export const logout = (req, res) => {
  res.json({
    data: "you hit the logout endpoint from authController",
  });
};
